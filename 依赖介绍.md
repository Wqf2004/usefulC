# 依赖介绍

## stdio.h

这个头文件包含C语言的标准输入输出函数，以及`FILE`文件流类型，适合存储文件流信息的对象类型。

## stdlib.h

这个头文件包含C语言的标准库函数，包括内存管理、数学函数、字符串处理函数等。

## string.h

这个头文件包含C语言的字符串处理函数，用于操作字符数组和字符串，提供了一系列用于复制、连接、比较和搜索字符串的函数。

## ctype.h

这个头文件包含C语言的字符类型函数，用于检查字符的类型和属性，如大小写转换、数字转换等。

## time.h

这个头文件包含C语言的时间函数，用于获取当前时间和日期，以及进行时间和日期操作。

## conio.h

这个头文件包含C语言的控制台输入输出函数，用于在控制台上显示和读取用户输入。该库定义的函数不具有跨平台性，仅适用于Windows平台。

## windows.h

这个头文件包含Windows操作系统相关的函数和宏定义，用于进行Windows API调用，支持Windows平台下的图形用户界面、系统服务和硬件交互等功能。
