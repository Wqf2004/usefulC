# C语言结构体

`typedef`是C语言中的一个关键字，用于给一个已有类型起一个别名。
`struct`也是C语言中的一个关键字，用于声明一个结构体类型。

结构体是对一个数据整体的封装，这个整体的一个部分称为结构体的数据成员。

假设你想要跟踪图书馆中书籍的动态，很显然书籍是一个非常具体的实体，而你只需要把握书籍的几个信息点就可以很好的跟踪[^跟踪]这本书了。
我认为你需要的信息点有：

- 书名（title）
- 作者（author）
- 主题（subject）
- 书的ID（book_id）

其中，书名、作者、主题都是字符串，而书的ID是一个整数。对于结构体中的字符串，通常采用的是预设大小的字符数组。

## 构建结构体

于是，用C语言来实现这个需求，可以这样：

```c
struct {
    char title[100];
    char author[100];
    char subject[100];
    int book_id;
} book0;
```

其他构建结构体的方式参见[构建图书结构体](./smms/struct_book.c)

## 初始化结构体变量

1. 直接初始化
2. 指定初始化
3. 结构体数组初始化

## 访问结构体的数据成员

对于一个结构体变量，可以通过`.`来访问其数据成员。

## 修改结构体的数据成员

对于基本数据类型，可以直接修改。
对于派生数据类型，如字符串，需要使用`strcpy`函数来修改。

[^跟踪]: 跟踪，在这里可以理解成“从图书馆的所有书籍中定位到某本书”，或者希望对图书馆中的书籍进行分类。
