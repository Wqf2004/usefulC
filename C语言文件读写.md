# C语言文件读写

C语言文件读写借助于`stdio.h`头文件中的`fopen`函数，该函数用于打开一个文件，并返回一个`FILE`的文本流类型的指针，用于后续的读写操作。
函数签名：

```c
FILE *fopen(const char *path, const char *mode);
// 使用给定的模式mode打开filename所指向的文件
```

`FILE`是一个不透明结构体类型，在现代windows系统中，无法直接访问其内部的成员变量。但是，它被设计为适合存储文件流信息的结构。

## 流（stream）的概念

**流（stream）** 是一种抽象概念，它的设计能够表示数据的流动方向和处理方式。
是程序与外部数据源（如文件、网络、键盘等）之间传输数据的通道。

在C语言中使用`FILE*`作为流的具体实现，一方面它本身适合存储流信息，另一方面`FLIE*`指针可以方便的传递和使用。

至于为什么要使用指针，我认为一方面是函数返回指针对于C语言的函数设计是一种很好的封装方式，另一方面对于文件流的操作无外乎是读取、写入、追加，这些操作的关键是的读取或写入位置的正确拾取，因此人们在使用文件流进行编程时，并不关心文件流中具体存了什么值，而是关心这个文件流对应的首地址（即其指针）。

## EOF文件结束符

在C语言中，文件结束符（EOF）是一个特殊的值，表示文件的结尾。在C语言中，EOF是一个整数，通常为-1。

同时有一个配套使用的函数`feof`，用于判断文件是否已经读取到文件的结尾。
它的函数签名是：

```c
int feof(FILE *stream);
// 测试给定流stream的文件结束末尾标识符（用于判断是否到结尾了）
// 如果stream的文件结束标识符被设置，则返回非零值，否则返回零
```

## 文件流的关闭

当文件流不再使用时，需要关闭文件流，以释放资源，并可防止系统对文件进行错误操作。关闭文件流使用`fclose`函数，其函数签名是：

```c
int fclose(FILE *stream);
// 关闭给定流stream，刷新所有缓冲区
// 成功返回0，失败返回EOF
```
