# C语言中的变量与内存开辟

C语言中的变量是内存的一块命名空间，用于存储数据。
任意一个C语言变量包括了以下几个要素：

- 名称（标识符）
- 类型（决定空间大小和解释方式）
- 值（实际存储的数据）
- 内存地址（变量在内存中的位置）

## 类型

C语言中的类型分为两种：

1. 基本类型（Fundamental Types）
是编译器内置的、不可再分解的原始类型，包括

    - int（整数类型）
    - float（单精度浮点数）
    - double（双精度浮点数）
    - char（字符类型）
    - void（空类型，无值）
2. 派生类型（Derived Types）
由基本类型组合或构造而成，依附于基本类型，包括：

    - 数组（Array）：多个相同类型变量的集合
    - 指针（Pointer）：存储变量内存地址的变量
    - 结构体（Struct）：用户自定义的数据类型
    - 共用体（Union）：共享内存空间的变量
    - 枚举（Enum）：命名的整型常量集合

值得注意的是，short, long, long long, unsigned int, unsigned char等都是基本类型，而`int*`是指针类型，原因是对于这些类型具有不同的大小和用途，它们的设计和使用基于语义和性能，是在编译器中内置的、不可再分解的，但是`int*`等指针类型则显然要依附于int等基本类型。

扩展：在Keil C51编译器中，bit也作为一种内置数据类型，用于声明一个位变量，只能存储0或1两种值，这是标准C语言中没有的。因为标准C语言的最小可寻址单位是字节（byte）。

## 内存开辟

C语言中的内存开辟分为两种：

1. 静态内存开辟：
*==对应的变量放在全局变量区，而不是在函数中定义的局部变量。==*
内存开辟时机：在编译时确定，程序启动时分配。
特点：内存大小固定，生命周期在程序运行期间一直存在。
2. 动态内存开辟：
内存开辟时机：在运行时通过函数动态申请。
特点：动态内存必须手动释放（free），否则会造成**内存泄露**[^内存泄漏]。

[^内存泄漏]: 内存泄漏是指程序运行过程中，由于疏忽或错误造成某些内存没有被释放，从而造成了内存的浪费，也可能导致程序崩溃。

## 函数中的变量

C语言中的函数定义的变量，在函数调用时，会被分配到栈中，在函数调用结束时，会被释放。
这种在程序运行期间动态创建和销毁的变量，称为**局部变量**。其中，函数中定义的变量和函数形参都是局部变量。

函数的形参在函数调用时会被赋予实参的值（或地址，如果是指针），并为其在栈（stack）上分配内存。即其值来源于实参，但独立于实参，除非通过指针间接操作，否则对形参的修改不会影响实参的值。

## 变量作用域

C语言中的变量作用域分为两种：

- 局部变量：在函数内部定义的变量。
- 全局变量：在函数外部定义的变量。

注意：main函数也是函数，所以main函数中定义的变量也是局部变量。

特殊：在函数中，使用static关键字修饰的变量，会被分配到静态存储区，在程序运行期间一直存在，即使函数调用结束也仍然存在。

局部变量存放在栈（stack）中，全局变量存放在静态存储区（.bss），静态存储区又分为数据存储区（.rodat/.dat）和堆存储区（heap）。动态内存开辟的变量存放在堆存储区。

## 变量生命周期

C语言中的变量生命周期分为两种：

- 静态生命周期：在程序运行期间一直存在。
- 动态生命周期：在程序运行期间动态创建和销毁。
