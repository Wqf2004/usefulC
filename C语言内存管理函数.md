# C语言内存管理函数

C语言内存管理，本质是动态内存管理，包括了内存的分配、释放、移动和复制等操作。

因为C语言的最小可寻址单位是字节（byte），所以内存管理函数的参数都是以字节为单位的。

要讨论内存，那么最基础的内存空间的一个数量关系就是内存的大小，前面知道内存的单位是byte，计算内存大小使用到C语言中内置的运算符`sizeof`，这个运算符可以用于获取一个变量或类型所占的字节数 [^sizeof]。

[^sizeof]: 注意`sizeof`后面的括号是语法要求，用于包围其操作数（类型、变量或表达式），告诉编译器“我要知道这个东西占多少字节”。尽管它看起来像函数调用，但本质是运算符，且括号不可省略。

## 内存管理函数

C语言中的内存管理函数，包括了以下几个：

- malloc：分配内存
    `void *malloc(int num);`
    动态分配内存，接收一个参数，表示要分配的字节数。返回值是分配的内存的起始地址。
- calloc：分配并清零内存
- realloc：重新分配内存
- free：释放内存
    `void free(void *address);`
    用于释放先前分配的内存，接收一个参数，表示要释放的内存的起始地址，并将该内存标记为未使用状态。返回值是void。
- memcpy：复制内存
- memset：设置内存
