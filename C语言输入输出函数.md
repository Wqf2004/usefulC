# C语言输入输出函数

C语言中定义了标准输入输出空间
stdin, stdout, stderr
它们分别对应与==标准输入==（默认指向键盘输入，通常为终端或控制台）、==标准输出==（默认指向屏幕输出，控制台）、==标准错误输出==（也默认指向屏幕，但与 stdout 分离，便于错误信息独立处理）。
它们本质是标准库`stdio.h`中预定义的文件流（`FILE*` 类型）。

同时，在这一标准库中还定义了丰富的处理输入输出的函数，主要分为两类：

1. 标准输入输出函数：用于处理标准输入输出，如键盘输入、显示输出等。
2. 文件输入输出函数：用于处理文件输入输出，如读写文件、处理文件等。

## 标准输入输出函数

标准输入输出函数用于处理键盘输入和屏幕输出，主要包括以下几个函数：

- `printf`：格式化输出函数，用于向屏幕输出数据。
    `int printf(const char *format, ...);`
    发送格式化输出到标准输出stdout
- `scanf`：格式化输入函数，用于从键盘读取数据。
- `getchar`：读取单个字符输入。
- `putchar`：输出单个字符。
- `gets`：读取字符串输入（不推荐使用，存在安全风险）。
- `puts`：输出字符串。

## 文件输入输出函数

文件输入输出函数用于处理文件输入输出，主要包括以下几个函数：

- `fopen`：打开文件。
- `fclose`：关闭文件。
- `fread`：读取文件。
- `fwrite`：写入文件。
- `fseek`：定位文件指针。
- `ftell`：获取文件指针位置。
- `rewind`：重置文件指针。
- `fgetc`：读取单个字符。
- `fputc`：写入单个字符。
- `fprintf`：将格式化的数据写入指定文件。
    `int fprintf(FILE *stream, const char *format, ...);`
    发送格式化输出到标准输出stdout
- `fgetc`：读取单个字符。
- `fputc`：写入单个字符。
- `fgets`：读取字符串。
    `char *fgets(char *str, int n, FILE *stream);`
    从指定的流 stream 读取一行，并把它存储在 str 所指向的字符串内。当读取 (n-1) 个字符时，或者*读取到换行符*时，或者到达文件末尾时，它会停止，具体视情况而定。如果成功，该函数返回相同的 str 参数。如果到达文件末尾或者没有读取到任何字符，str 的内容保持不变，并返回一个空指针。如果发生错误，返回一个空指针。在*读取一行数据*后，会自动将文件指针（file pointer）向后移动，指向刚刚读取内容的下一个位置。
- `fputs`：写入字符串。
- `ungetc`：回退单个字符。
