# C语言中的指针

C语言中的指针是一种特殊的变量，它存储的是另一个变量的地址。

C语言中的大多数底层实现的函数，都是基于指针来实现的。有以下几个原因：

1. 指针可以直接操作内存，避免了数据拷贝的开销。
2. 指针允许函数以统一的接口处理不同数据类型或结构。
3. 指针可以方便的实现动态内存分配。

## 指针与回调机制

指针可以用于实现回调机制。回调机制是一种常见的编程模式，它允许函数在运行时动态地调用另一个函数。

指针可指向函数（函数指针），这是回调机制的基础。

回调是一种“通知”机制：你把一个函数交给另一个函数，当特定事件发生时，后者调用你提供的函数。
